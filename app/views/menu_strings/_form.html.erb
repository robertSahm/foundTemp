<%= form_for(@menu_string) do |f| %>
  <% if @menu_string.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@menu_string.errors.count, "error") %> prohibited this menu_string from being saved:</h2>

      <ul>
      <% @menu_string.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :version %><br />
    <%= f.number_field :version %>
  </div>
  <div class="field">
    <%= f.label :provider_id %><br />
    <%= f.number_field :provider_id %>
  </div>
  <div class="field">
    <%= f.label :full_address %><br />
    <%= f.text_field :full_address %>
  </div>
  <div class="field">
    <%= f.label :menu %><br />
    <%= f.text_area :menu %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
